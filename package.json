{
  "name": "prints",
  "version": "1.0.0",
  "description": "prints",
  "main": "index.js",
  "scripts": {
    "build": "bundle exec middleman build",
    "deploy": "npm run build && git-directory-deploy --directory build --repo staging",
    "deploy:prod": "npm run build && git-directory-deploy --directory build --branch master --repo production"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/jayperryworks/prints.git"
  },
  "author": "Jay Perry Works",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/jayperryworks/prints/issues"
  },
  "homepage": "https://github.com/jayperryworks/prints",
  "devDependencies": {
    "git-directory-deploy": "^1.3.0",
    "svgo": "^0.5.0"
  },
  "config": {}
}
