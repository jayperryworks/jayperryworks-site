<%
  # Cover image component
  # -> an image that scales to viewport size

  # Locals:
  # source - STRING: the path to the image
  alt ||= '' # STRING: alt attribute, if any
  classes ||= '' # STRING (optional): any other classes you'd like to pass in
%>

<%= image_tag source,
  data: { 'ui-cover': '' },
  alt: alt
%>
